Form to send the solutions to the Red Hat–PyCon CZ Coding Challenge

# How to deploy:

    $ python3 -m venv venv
    $ source venv/bin/activate
    $ pip install -r requirements.txt
    $ python form.py serve

